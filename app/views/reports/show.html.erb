<h1>Relatório do Documento Fiscal</h1>

<h2>Dados do Documento Fiscal</h2>
<p><strong>Número de Série:</strong> <%= @fiscal_document.serie %></p>
<p><strong>Número da Nota Fiscal:</strong> <%= @fiscal_document.nNF %></p>
<p><strong>Data e Hora de Emissão:</strong> <%= @fiscal_document.dhEmi %></p>

<h3>Emitente</h3>
<p><strong>Nome:</strong> <%= @fiscal_document.emitente['nome'] %></p>
<p><strong>CNPJ:</strong> <%= @fiscal_document.emitente['cnpj'] %></p>
<p><strong>Endereço:</strong> <%= @fiscal_document.emitente['endereco'] %></p>

<h3>Destinatário</h3>
<p><strong>Nome:</strong> <%= @fiscal_document.destinatario['nome'] %></p>
<p><strong>CNPJ:</strong> <%= @fiscal_document.destinatario['cnpj'] %></p>
<p><strong>Endereço:</strong> <%= @fiscal_document.destinatario['endereco'] %></p>

<h2>Produtos Listados</h2>
<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>NCM</th>
      <th>CFOP</th>
      <th>Unidade</th>
      <th>Quantidade</th>
      <th>Valor Unitário</th>
    </tr>
  </thead>
  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.ncm %></td>
        <td><%= product.cfop %></td>
        <td><%= product.unit %></td>
        <td><%= product.quantity %></td>
        <td><%= product.value %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<h2>Impostos Associados</h2>
<% if @taxes.any? %>
  <p><strong>ICMS:</strong> <%= @taxes.first.icms %></p>
  <p><strong>IPI:</strong> <%= @taxes.first.ipi %></p>
  <p><strong>PIS:</strong> <%= @taxes.first.pis %></p>
  <p><strong>COFINS:</strong> <%= @taxes.first.cofins %></p>
<% else %>
  <p>Sem impostos associados.</p>
<% end %>

<%= link_to 'Voltar para o upload', new_report_path, class: 'btn btn-primary' %>
